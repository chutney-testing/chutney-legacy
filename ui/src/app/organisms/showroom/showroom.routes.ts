import { Routes } from '@angular/router';
import { ShowroomComponent } from './showroom.component';

export const ShowroomRoute: Routes = [
    {
        path: '',
        component: ShowroomComponent
    }
];
