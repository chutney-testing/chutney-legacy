import { AccordionToggleDirective } from './accordion-toggle.directive';

describe('AccordionToggleDirective', () => {
  it('should create an instance', () => {
    const directive = new AccordionToggleDirective();
    expect(directive).toBeTruthy();
  });
});
