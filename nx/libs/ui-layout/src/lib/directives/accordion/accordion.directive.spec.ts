import { AccordionDirective } from './accordion.directive';

describe('AccordionDirective', () => {
  it('should create an instance', () => {
    const directive = new AccordionDirective(null);
    expect(directive).toBeTruthy();
  });
});
