import { AppBannerDirective } from './app-banner.directive';

describe('AppBannerDirective', () => {
  it('should create an instance', () => {
    const directive = new AppBannerDirective();
    expect(directive).toBeTruthy();
  });
});
